import { ROUTE_PATHS } from './route-paths.constants';

export const INDEXABLE_PATHS = [
  ROUTE_PATHS.home,
  ROUTE_PATHS.about,
  ROUTE_PATHS.features,
  ROUTE_PATHS.faq,
  ROUTE_PATHS.contact,
] as const;
